User stories to implement:

1. I can submit a FormData object that includes a file upload.
2. When I submit something, I will receive the file size in bytes within the JSON response.

Package:
https://www.npmjs.com/package/multer

Steps.
1. Created an html view that is served on get as a way to upload. [done]
2. Created the post method via the input that allows to upload the file.  [done]
3. On submission, the client is served a json with the filesize. [done]
